# GTAI — Next-Generation AI Talents

High-fidelity frontend prototype for the GTAI digital talent agency.

## Local preview

Serve this directory with any static web server, then open `index.html`.

Prototype access credentials are intentionally client-side and are not production security.

## Deployment

This repository is designed to be deployed as a static site with the repository root as the publish directory.
